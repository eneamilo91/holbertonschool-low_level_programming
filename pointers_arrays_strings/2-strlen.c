#include"main.h"
/**
 * _strlen -entry point,returns length of string
 * @s: -holds value for input
 * Return:0
 */


	int _strlen(char *s)
{
	int i;

	for (i = 0 ; s[i] != '\0'; i++)
	{


	}
	return (i);
}
