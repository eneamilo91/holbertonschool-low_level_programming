#include"main.h"

#include "main.h"
/**
 * add - Entry point
 * @first_x: -first number
 * @second_y: second num
 * Return the sum of two int numbers;
 * Return: 0
 */

	int add(int x, int y)
{
	return (x + y);
}
