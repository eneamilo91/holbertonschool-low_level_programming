#include "main.h"

/**
 * _abs - function computes the absolute value of the integer argument j
 * @a: the character to check
 * Return: 1 a is greater or equal zero,  -1 if a is less then zero
 */

	int _abs(int a)
{

	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}

}
